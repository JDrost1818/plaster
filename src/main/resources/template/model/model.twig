package {{ package }};

import javax.persistence.*

{{ dependencies }}

{{ header }}
@Entity
public class {{ className }} {

    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    {{ idField }}

    {{ fields }}

    {{ methods }}

}
